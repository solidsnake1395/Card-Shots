framework:
    secret: '%env(APP_SECRET)%'

    # ✅ CSRF protection activada
    csrf_protection: true

    # ✅ Configuración segura y explícita de sesiones
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
        save_path: '%env(resolve:PHP_SESSION_SAVE_PATH)%'