<form method="post" action="{{ path('app_carta_delete', {'id': cartum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cartum.id) }}">
    <button class="btn">Delete</button>
</form>
