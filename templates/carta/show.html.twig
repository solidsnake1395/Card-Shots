{% extends 'base.html.twig' %}

{% block title %}Carta{% endblock %}

{% block body %}
    <h1>Carta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cartum.id }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ cartum.texto }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ cartum.titulo }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ cartum.imagen }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ cartum.categoria }}</td>
            </tr>
            <tr>
                <th>EsComodin</th>
                <td>{{ cartum.esComodin ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_carta_index') }}">back to list</a>

    <a href="{{ path('app_carta_edit', {'id': cartum.id}) }}">edit</a>

    {{ include('carta/_delete_form.html.twig') }}
{% endblock %}
